# tarraco-rois configuration

project_name: tarraco-rois
version: "0.1.0"

# Paths (relative to project root)
ortho_path: sandbox/data/ortho/
output_path: dist/masks/
tiles_path: res/data/tiles/

# Default module
default_module: langsam

# Docker settings
docker:
  image: tarraco-rois
  gpu: true
